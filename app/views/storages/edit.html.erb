<%= form_with model: @storage, url: storage_path(@storage), local:true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "名前変更" %>
<% end %>
<%= link_to '削除', storage_path(@storage), method: :delete, data: { confirm: "この保管庫を削除しますか？" } %>